apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eazybank

bases:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: nginx-frontend
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1

commonLabels:
  env: development
